import { motion } from 'motion/react';

export default function Stats() {
  const stats = [
    { value: "US$ 700 MM+", label: "Millones financiados" },
    { value: "180.000 +", label: "Facturas operadas" },
    { value: "3.000 +", label: "Clientes" }
  ];

  return (
    <section className="relative z-20 -mt-16 mb-20 px-4">
      <div className="max-w-5xl mx-auto bg-white rounded-[3rem] shadow-xl border border-gray-100 p-8 md:p-12">
        <div className="grid grid-cols-1 md:grid-cols-3 gap-8 divide-y md:divide-y-0 md:divide-x divide-gray-100">
          {stats.map((stat, index) => (
            <motion.div 
              key={index}
              initial={{ opacity: 0, y: 20 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ delay: index * 0.1 }}
              className="text-center px-4 pt-6 md:pt-0 first:pt-0"
            >
              <h3 className="text-3xl md:text-4xl font-extrabold text-black mb-2">{stat.value}</h3>
              <p className="text-brand-primary font-medium text-xl">{stat.label}</p>
            </motion.div>
          ))}
        </div>
      </div>
    </section>
  );
}